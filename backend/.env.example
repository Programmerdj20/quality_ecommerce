# ==================================
# STRAPI CONFIGURATION
# ==================================
HOST=0.0.0.0
PORT=1337
APP_KEYS="generateThisWithOpenssl,generateThisWithOpenssl,generateThisWithOpenssl,generateThisWithOpenssl"
API_TOKEN_SALT=generateThisWithOpenssl
ADMIN_JWT_SECRET=generateThisWithOpenssl
TRANSFER_TOKEN_SALT=generateThisWithOpenssl
JWT_SECRET=generateThisWithOpenssl

# ==================================
# DATABASE (PostgreSQL para Heroku)
# ==================================
DATABASE_CLIENT=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=quality_ecommerce
DATABASE_USERNAME=tu_usuario
DATABASE_PASSWORD=tu_password
DATABASE_SSL=false

# En producción Heroku, usar:
# DATABASE_URL=postgresql://user:password@host:port/database
# DATABASE_SSL=true

# ==================================
# API CONTABLE (para validaciones desde Strapi)
# ==================================
API_CONTABLE_URL=https://tu-api-contable.herokuapp.com
API_CONTABLE_TOKEN=tu_token_de_api_aqui

# ==================================
# INSTRUCCIONES PARA GENERAR SECRETS
# ==================================
# Ejecuta estos comandos en tu terminal para generar valores aleatorios seguros:
#
# APP_KEYS (genera 4):
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
# Otros secrets (API_TOKEN_SALT, ADMIN_JWT_SECRET, TRANSFER_TOKEN_SALT, JWT_SECRET):
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
