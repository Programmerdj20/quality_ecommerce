---
/**
 * Checkout Pending Page - Página de pago pendiente
 *
 * Se muestra cuando el pago está en proceso de aprobación
 */

import BaseLayout from '@/layouts/BaseLayout.astro';

// Obtener parámetros de la URL
const { url } = Astro;
const paymentId = url.searchParams.get('payment_id');
const externalReference = url.searchParams.get('external_reference');
---
<BaseLayout
  title="Pago Pendiente - Quality Ecommerce"
  description="Tu pago está siendo procesado"
>
  <div class="min-h-screen bg-gray-50 py-12">
    <div class="container mx-auto px-4">
      <div class="max-w-2xl mx-auto">
        <!-- Card de Pendiente -->
        <div class="bg-white rounded-2xl shadow-lg p-8 md:p-12 text-center">
          <!-- Ícono de Pendiente -->
          <div class="w-24 h-24 bg-yellow-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <svg class="w-12 h-12 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>

          <!-- Título -->
          <h1 class="font-heading font-bold text-3xl md:text-4xl text-gray-800 mb-4">
            Pago Pendiente
          </h1>

          <!-- Mensaje -->
          <p class="text-lg text-gray-600 mb-8">
            Tu pago está siendo procesado. Te notificaremos por email cuando sea confirmado.
          </p>

          <!-- Información del Pedido -->
          {externalReference && (
            <div class="bg-gray-50 rounded-lg p-6 mb-8">
              <p class="text-sm text-gray-600 mb-2">Número de Pedido:</p>
              <p class="font-mono font-bold text-xl text-gray-800">{externalReference}</p>
            </div>
          )}

          {paymentId && (
            <div class="bg-gray-50 rounded-lg p-6 mb-8">
              <p class="text-sm text-gray-600 mb-2">ID de Pago:</p>
              <p class="font-mono text-gray-800">{paymentId}</p>
            </div>
          )}

          <!-- Información Importante -->
          <div class="bg-blue-50 border-l-4 border-blue-500 p-6 mb-8 text-left">
            <div class="flex gap-3">
              <svg class="w-6 h-6 text-blue-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              <div>
                <h3 class="font-bold text-gray-800 mb-2">¿Por qué está pendiente?</h3>
                <p class="text-sm text-gray-600">
                  Algunos métodos de pago requieren tiempo adicional para ser procesados.
                  Esto puede tomar desde unos minutos hasta 2 días hábiles, dependiendo del método utilizado.
                </p>
              </div>
            </div>
          </div>

          <!-- Próximos Pasos -->
          <div class="border-t border-gray-200 pt-8 mb-8">
            <h2 class="font-heading font-bold text-xl text-gray-800 mb-4">
              ¿Qué sigue?
            </h2>
            <div class="text-left space-y-3">
              <div class="flex items-start gap-3">
                <div class="w-6 h-6 bg-gray-800 text-white rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-xs font-bold">1</span>
                </div>
                <p class="text-gray-600">Recibirás un email cuando tu pago sea confirmado</p>
              </div>
              <div class="flex items-start gap-3">
                <div class="w-6 h-6 bg-gray-800 text-white rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-xs font-bold">2</span>
                </div>
                <p class="text-gray-600">Una vez confirmado, procesaremos tu pedido inmediatamente</p>
              </div>
              <div class="flex items-start gap-3">
                <div class="w-6 h-6 bg-gray-800 text-white rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-xs font-bold">3</span>
                </div>
                <p class="text-gray-600">Si tu pago es rechazado, te notificaremos de inmediato</p>
              </div>
            </div>
          </div>

          <!-- Botones de Acción -->
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href="/"
              class="inline-block bg-gradient-to-r from-gray-800 to-black text-white font-bold py-3 px-8 rounded-lg hover:from-black hover:to-gray-900 transition-all duration-300 transform hover:scale-105"
            >
              Volver al Inicio
            </a>
            <a
              href="/productos"
              class="inline-block bg-white text-gray-800 font-bold py-3 px-8 rounded-lg border-2 border-gray-800 hover:bg-gray-50 transition-all duration-300"
            >
              Seguir Comprando
            </a>
          </div>

          <!-- Soporte -->
          <div class="mt-8 pt-8 border-t border-gray-200">
            <p class="text-sm text-gray-600">
              ¿Necesitas ayuda? <a href="/contacto" class="text-gray-800 font-semibold hover:underline">Contáctanos</a>
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  import { clearCart } from '@/stores/cartStore';
  import { resetCheckoutState } from '@/stores/orderStore';

  // Limpiar carrito y resetear estado del checkout cuando se carga la página
  document.addEventListener('DOMContentLoaded', () => {
    setTimeout(() => {
      clearCart();
      resetCheckoutState();
    }, 1000);
  });
</script>
