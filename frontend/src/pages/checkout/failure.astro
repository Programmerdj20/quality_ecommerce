---
/**
 * Checkout Failure Page - Página de pago fallido
 *
 * Se muestra cuando el pago fue rechazado o hubo un error
 */

import BaseLayout from '@/layouts/BaseLayout.astro';

// Obtener parámetros de la URL
const { url } = Astro;
const paymentId = url.searchParams.get('payment_id');
const externalReference = url.searchParams.get('external_reference');
---

<BaseLayout
  title="Error en el Pago - Quality Ecommerce"
  description="Hubo un problema con tu pago"
>
  <div class="min-h-screen bg-gray-50 py-12">
    <div class="container mx-auto px-4">
      <div class="max-w-2xl mx-auto">
        <!-- Card de Error -->
        <div class="bg-white rounded-2xl shadow-lg p-8 md:p-12 text-center">
          <!-- Ícono de Error -->
          <div class="w-24 h-24 bg-red-100 rounded-full flex items-center justify-center mx-auto mb-6">
            <svg class="w-12 h-12 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
            </svg>
          </div>

          <!-- Título -->
          <h1 class="font-heading font-bold text-3xl md:text-4xl text-gray-800 mb-4">
            Error en el Pago
          </h1>

          <!-- Mensaje -->
          <p class="text-lg text-gray-600 mb-8">
            Lo sentimos, tu pago no pudo ser procesado. No se realizó ningún cargo a tu cuenta.
          </p>

          <!-- Información del Pedido -->
          {externalReference && (
            <div class="bg-gray-50 rounded-lg p-6 mb-8">
              <p class="text-sm text-gray-600 mb-2">Número de Pedido:</p>
              <p class="font-mono font-bold text-xl text-gray-800">{externalReference}</p>
            </div>
          )}

          <!-- Razones Comunes -->
          <div class="bg-orange-50 border-l-4 border-orange-500 p-6 mb-8 text-left">
            <div class="flex gap-3">
              <svg class="w-6 h-6 text-orange-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
              </svg>
              <div>
                <h3 class="font-bold text-gray-800 mb-2">Razones Comunes</h3>
                <ul class="text-sm text-gray-600 space-y-1 list-disc list-inside">
                  <li>Fondos insuficientes</li>
                  <li>Datos de tarjeta incorrectos</li>
                  <li>Tarjeta vencida o bloqueada</li>
                  <li>Límite de compra excedido</li>
                  <li>Problema de conexión durante el proceso</li>
                </ul>
              </div>
            </div>
          </div>

          <!-- Recomendaciones -->
          <div class="border-t border-gray-200 pt-8 mb-8">
            <h2 class="font-heading font-bold text-xl text-gray-800 mb-4">
              ¿Qué puedes hacer?
            </h2>
            <div class="text-left space-y-3">
              <div class="flex items-start gap-3">
                <div class="w-6 h-6 bg-gray-800 text-white rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-xs font-bold">1</span>
                </div>
                <p class="text-gray-600">Verifica los datos de tu método de pago</p>
              </div>
              <div class="flex items-start gap-3">
                <div class="w-6 h-6 bg-gray-800 text-white rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-xs font-bold">2</span>
                </div>
                <p class="text-gray-600">Intenta con otro método de pago</p>
              </div>
              <div class="flex items-start gap-3">
                <div class="w-6 h-6 bg-gray-800 text-white rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <span class="text-xs font-bold">3</span>
                </div>
                <p class="text-gray-600">Contacta a tu banco si el problema persiste</p>
              </div>
            </div>
          </div>

          <!-- Botones de Acción -->
          <div class="flex flex-col sm:flex-row gap-4 justify-center">
            <a
              href="/checkout"
              class="inline-block bg-gradient-to-r from-gray-800 to-black text-white font-bold py-3 px-8 rounded-lg hover:from-black hover:to-gray-900 transition-all duration-300 transform hover:scale-105"
            >
              Intentar Nuevamente
            </a>
            <a
              href="/"
              class="inline-block bg-white text-gray-800 font-bold py-3 px-8 rounded-lg border-2 border-gray-800 hover:bg-gray-50 transition-all duration-300"
            >
              Volver al Inicio
            </a>
          </div>

          <!-- Soporte -->
          <div class="mt-8 pt-8 border-t border-gray-200">
            <p class="text-sm text-gray-600">
              ¿Necesitas ayuda? <a href="/contacto" class="text-gray-800 font-semibold hover:underline">Contáctanos</a>
            </p>
          </div>
        </div>

        <!-- Información de Seguridad -->
        <div class="mt-8 bg-blue-50 rounded-lg p-6">
          <div class="flex items-start gap-3">
            <svg class="w-6 h-6 text-blue-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
            </svg>
            <div class="text-left">
              <h3 class="font-bold text-gray-800 mb-1">Tu Seguridad es Importante</h3>
              <p class="text-sm text-gray-600">
                No se realizó ningún cargo a tu cuenta. Tus datos están protegidos con encriptación SSL.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</BaseLayout>

<script>
  import { resetCheckoutState } from '@/stores/orderStore';

  // Resetear estado del checkout cuando se carga la página
  document.addEventListener('DOMContentLoaded', () => {
    setTimeout(() => {
      resetCheckoutState();
    }, 1000);
  });
</script>
